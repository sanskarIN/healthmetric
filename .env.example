# HealthMetric currently requires no runtime secrets or network credentials.
# Keep this file as the documented placeholder for future local-only configuration.
# Never place real API keys, signing passwords, tokens, or private endpoints in Git.

HEALTHMETRIC_ENV=development
